<%= simple_form_for [@cocktail, @dose] do |f| %>
<%= f.input :description %>
<%= f.association :ingredient, collection: Ingredient.all %>
<%= f.button :submit %>
<%= link_to "Cancel", cocktail_path(@cocktail), {:class=>"btn btn-default"} %>
<% end %>

